// import { QueryOptions, useQuery } from '@tanstack/react-query';
// import { NodesService } from '@/services/Node/node.service';

// export const KEY_GET_NODE = 'getNode';

// export const useNodeDetails = (slug: string, options?: QueryOptions) =>
//   useQuery({
//     queryKey: [KEY_GET_NODE, slug],
//     queryFn: () => NodesService.getNodeDetails(slug),
//     ...options,
//     enabled: !!slug,
//   });

"QUERY EXAMPLE"